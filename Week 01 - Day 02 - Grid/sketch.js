

    let val = 4;
function setup(){
    createCanvas(500, 500);
    background(127, 127, 127 );
    angleMode(DEGREES);
    rectMode(CENTER);
}

function draw(){
    // translate(width/2, height/2);
    // push()

    // rotate(val++)
   gridBoxes(2, color(75, 75, 255), 2);
    // pop()
}